/**
 * Payment page: simulate payment, then redirect to thank-you.
 * Will be implemented in Step 6.
 */
(function () {
  var el = document.getElementById('payment-root');
  if (el) el.innerHTML = '<p class="empty-state">Payment form will appear here.</p>';
})();
